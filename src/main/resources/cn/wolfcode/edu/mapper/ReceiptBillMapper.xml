<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.wolfcode.edu.mapper.ReceiptBillMapper" >
  <resultMap id="BaseResultMap" type="cn.wolfcode.edu.domain.ReceiptBill" >
    <id column="id" property="id" />
    <result column="receiptTime" property="receipttime" />
    <result column="receiptMoney" property="receiptmoney" />
    <result column="billNum" property="billnum" />
    <result column="remark" property="remark" />
    <result column="totalMoney" property="totalmoney" />

    <!--学员-->

    <!--课程-->

    <!--支付方式-->

    <!--班级-->

    <!--收款人-->

    <!--营销人员-->

    <!--班级变动-->
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long" >
    delete from `receiptbill`
    where id = #{id}
  </delete>
  <insert id="insert" parameterType="cn.wolfcode.edu.domain.ReceiptBill" useGeneratedKeys="true" keyProperty="id" >
    insert into `receiptbill` (client_id, receiptTime, receiptMoney, course_id, payment_id, 
      billNum, grade_id, remark, receipter_id, marketer_id, classTrend_id, 
      totalMoney)
    values (#{client.id}, #{receipttime}, #{receiptmoney}, #{course.id}, #{payment.id},
      #{billnum}, #{grade.id}, #{remark}, #{receipter.id}, #{marketer.id}, #{classtrend.id},
      #{totalmoney})
  </insert>
  <update id="updateByPrimaryKey" parameterType="cn.wolfcode.edu.domain.ReceiptBill" >
    update `receiptbill`
    set client_id = #{client.id},
      receiptTime = #{receipttime},
      receiptMoney = #{receiptmoney},
      course_id = #{course.id},
      payment_id = #{payment.id},
      billNum = #{billnum},
      grade_id = #{grade.id},
      remark = #{remark},
      receipter_id = #{receipter.id},
      marketer_id = #{marketer.id},
      classTrend_id = #{classtrend.id},
      totalMoney = #{totalmoney}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long" >
    select id, client_id, receiptTime, receiptMoney, course_id, payment_id, billNum, 
    grade_id, remark, receipter_id, marketer_id, classTrend_id, totalMoney
    from `receiptbill`
    where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select id, client_id, receiptTime, receiptMoney, course_id, payment_id, billNum, 
    grade_id, remark, receipter_id, marketer_id, classTrend_id, totalMoney
    from `receiptbill`
  </select>

  <select id="queryForCount" resultType="int">
    select count(id)
    from `receiptbill`
  </select>

  <select id="queryForList" resultMap="BaseResultMap">
    select id, client_id, receiptTime, receiptMoney, course_id, payment_id, billNum,
    grade_id, remark, receipter_id, marketer_id, classTrend_id, totalMoney
    from `receiptbill`
    limit #{startIndex},#{rows}
  </select>
</mapper>